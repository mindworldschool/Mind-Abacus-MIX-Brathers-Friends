# Анализ логики вычитания в FriendsExampleGenerator

## Исходная проблема

Пользователь указал: "в 23-7 ты не можешь такой пример сгенерировать так как для этого нужно воспользоваться правилом Братья, т.к у тебя в единицах активны 3 бусины и тебе нужно прибавить еще три, но это не возможно поэтому нужно воспользоваться правилом Друзья, но в данном блоке такие действия запрещены, Так можно только в Микс."

## Анализ ситуации

### Пример: 23-7 (состояние 23, действие -7)

#### Разбор на абакусе:
- **Начальное состояние**: 23 (единицы=3, десятки=2)
- **Действие**: -7
- **Целевой разряд**: единицы (3)

#### Варианты выполнения -7 из единиц=3:

1. **Простой способ**: 3 - 7 = невозможно (не хватает бусин) ❌
2. **Через Братья**: -7 = -5 -2, но 3 < 5 (невозможно) ❌
3. **Через Друзья**: -7 = -10 +3
   - Вычесть 10 из десятков
   - **Добавить 3 к единицам**: 3 → 6
   - **ПРОБЛЕМА**: для добавления 3 к 3 на абакусе:
     - Текущее: U=0, L=3
     - Целевое: U=1, L=1
     - Изменение: dU=+1, dL=-2
     - Это требует **правило Братья**: +5-2
   - **Вывод**: -7 из состояния 3 = **Друзья + Братья = MIX** ❌

### Проверка кода

#### `_getSubtractionRequirements(7)` возвращает: `[0, 1, 5, 6]`

**Состояние 3 НЕ входит в список!** ✅

#### Почему именно эти состояния?

Для вычитания -7 через Friends (-10 +3) нужно:
- `_canPlusDirect(targetVal, 3)` = true (можно добавить 3 БЕЗ Brothers)

Проверка для каждого состояния:
- **0**: _canPlusDirect(0, 3) = true ✅ (0→3, добавить 3 нижних)
- **1**: _canPlusDirect(1, 3) = true ✅ (1→4, добавить 3 нижних)
- **2**: _canPlusDirect(2, 3) = false ❌ (2→5, требует Brothers +5-2)
- **3**: _canPlusDirect(3, 3) = false ❌ (3→6, требует Brothers +5-2)
- **4**: _canPlusDirect(4, 3) = false ❌ (4→7, требует Brothers +5-2)
- **5**: _canPlusDirect(5, 3) = true ✅ (5→8, добавить 3 нижних)
- **6**: _canPlusDirect(6, 3) = true ✅ (6→9, добавить 3 нижних)
- **7**: 7+3=10 > 9 ❌

**Список [0,1,5,6] ПРАВИЛЬНЫЙ!** ✅

## Результаты проверки

### ✅ Тест 1: digitCount=1, состояние 23
- **Результат**: действие -7 **НЕ создано** ✅
- **Причина**: единицы (3) не входят в `_getSubtractionRequirements(7)` = [0,1,5,6]

### ✅ Тест 2: digitCount=2, состояние 23
- **Результат**: действие -X7 **НЕ создано** ✅
- **Причина**: десятки (2) не входят в список [0,1,5,6]

### ✅ Тест 3: digitCount=2, состояние 13
- **Результат**: действие -X7 **НЕ создано** ✅
- **Причина**: десятки (1) подходят, НО `_buildMultiDigitAction` не может подобрать цифру для единиц (3)

## Внесенные исправления

### 1. Улучшение `_buildMultiDigitAction` (строка 626-662)

**До исправления:**
```javascript
} else if (!isAddition && d === 0) {
  possibleDigits.push(0);
} else if (!isAddition && d > 0 && this._canMinusDirect(currentVal, d)) {
  possibleDigits.push(d);
}
```

**После исправления:**
```javascript
if (d === 0) {
  // d=0 означает "не изменять этот разряд" - всегда разрешено
  possibleDigits.push(0);
} else if (isAddition && this._canPlusDirect(currentVal, d)) {
  possibleDigits.push(d);
} else if (!isAddition && this._canMinusDirect(currentVal, d)) {
  possibleDigits.push(d);
}
```

**Изменения:**
- Явно разрешен `d=0` для обоих направлений (сложение и вычитание)
- Добавлены комментарии для ясности
- Убрана избыточная проверка `d === 0` только для вычитания

### 2. Улучшенные комментарии

Добавлены пояснения в код:
- "🔥 КРИТИЧНО: Проверяем ВСЕ цифры, не только целевой разряд"
- "Для нецелевых разрядов НЕ ДОЛЖНЫ требоваться Brothers/Friends"

## Заключение

**Код FriendsExampleGenerator работает ПРАВИЛЬНО!** ✅

Логика проверки состояний:
1. Проверяется **целевой разряд** через `_getSubtractionRequirements` / `_getAdditionRequirements`
2. Проверяется возможность применения формулы через `_canPlusDirect` / `_canMinusDirect`
3. В `_buildMultiDigitAction` проверяются **ВСЕ нецелевые разряды** на возможность изменения БЕЗ Brothers/Friends

**Пример 23-7 НЕ генерируется в блоке Friends** - это правильное поведение, так как такой пример требует комбинации правил Friends + Brothers, что разрешено только в блоке MIX.

